apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: my-ds
  labels:
    app: monitoring
spec:
  template:
    metadata:
      labels:
        app: monitoring
    spec:
      containers:
        - name: monitoring
          image: cloudwatch
          ports:
            - containerPort: 80
  selector:
    app: monitoring
    