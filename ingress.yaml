apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: 
  name: my-ingress
spec:
  ingressClassName: 
  rules:
    - http:
        paths:
        - path: /mobile
          pathType: Prefix
          backend:
            service:
              name: mobile
              port:
                number: 80
